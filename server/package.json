{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "json-server --watch videos.json --port 9000 --middlewares ./middleware/random.js"
  },
  "dependencies": {
    "json-server": "^0.17.1",
    "nodemon": "^2.0.20"
  }
}
